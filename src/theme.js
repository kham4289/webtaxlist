import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  
  typography: {
    fontFamily: ['"Noto sans lao"', "Open Sans"].join(","),
    color: "#000000"
  },
});

export default theme;
